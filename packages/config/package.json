{
  "name": "@interview/config",
  "version": "0.1.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "lint": "eslint src/**/*.ts*",
    "type-check": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "styled-components": "catalog:"
  },
  "devDependencies": {
    "@types/styled-components": "catalog:",
    "eslint": "catalog:",
    "typescript": "catalog:"
  }
}