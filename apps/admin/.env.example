# Admin App 环境变量示例文件
# 复制此文件为 .env.local 并填写实际值
# ==========================================

# 公共 URL 前缀（NEXT_PUBLIC_ 开头的会暴露给浏览器）
# ==========================================

# Web App URL（用于预览文档）
NEXT_PUBLIC_WEB_URL=http://localhost:3000

# Admin App URL（当前应用）
NEXT_PUBLIC_ADMIN_URL=http://localhost:3003

# FastAPI 后端服务 URL
FASTAPI_URL=http://localhost:8081

# ==========================================
# NextAuth 配置
# ==========================================

# NextAuth 密钥（用于加密 session）
# 生成命令: openssl rand -base64 32
NEXTAUTH_SECRET=your-nextauth-secret-here

# NextAuth URL（生产环境需要设置）
NEXTAUTH_URL=http://localhost:3003

# 管理员邮箱列表（逗号分隔，用于 OAuth 登录后的权限判断）
NEXTAUTH_ADMIN_EMAILS=admin@example.com,another-admin@example.com

# ==========================================
# 用户凭据配置（邮箱密码登录）
# ==========================================

# Admin 用户（推荐使用 bcrypt hash）
# 生成 bcrypt hash: node -e "console.log(require('bcryptjs').hashSync('your-password', 12))"
ADMIN_EMAIL=your-admin@example.com
NEXTAUTH_ADMIN_PASSWORD_HASH=$2a$12$your-bcrypt-hash-here
# 开发环境可用明文密码（不推荐生产环境）
NEXTAUTH_ADMIN_PASSWORD=your-password

# 普通用户（可选）
USER_EMAIL=user@example.com
USER_PASSWORD_HASH=$2a$12$your-bcrypt-hash-here
USER_PASSWORD=your-password

# ==========================================
# FastAPI 登录凭据（作为 admin 用户的代理）
# ==========================================

ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin123

# ==========================================
# OAuth 配置（可选）
# ==========================================

# Google OAuth
# 从 https://console.cloud.google.com 获取
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret

# GitHub OAuth
# 从 https://github.com/settings/developers 获取
GITHUB_CLIENT_ID=your-github-client-id
GITHUB_CLIENT_SECRET=your-github-client-secret

# ==========================================
# CSRF 保护配置
# ==========================================

CSRF_SECRET=your-csrf-secret-here

# ==========================================
# Redis 配置（可选）
# ==========================================

REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=your-redis-password
REDIS_DB=0

# ==========================================
# 文档操作日志 API 密钥（可选）
# ==========================================

DOC_LOG_API_KEY=your-doc-log-api-key

# ==========================================
# 服务配置
# ==========================================

PORT=3003
HOST=localhost
PROTOCOL=http

# ==========================================
# 部署环境配置
# ==========================================

# Vercel 部署标识（部署到 Vercel 时自动设置）
# VERCEL=1
